version: 2

models:
  - name: seven_week_active_customers
    columns:
      - name: customer_id
        tests:
          - unique
          - not_null
      - name: avg_order_amount
        tests:
          - not_null
      - name: order_count
        tests:
          - not_null